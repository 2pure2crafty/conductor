# HDS Router

Private on-demand agent router for the HDS pipeline. A small PHP app, reachable
only over Tailscale, that lets Patch spin up and wrap down Claude Code agent
sessions from a phone browser instead of leaving them running 24/7.

> **Note:** this repo is public temporarily. It will be set to private soon.
> See `LICENSE` — no usage rights are granted while it's public.
